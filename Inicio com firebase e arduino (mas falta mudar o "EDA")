<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>SENZ – Início</title>
  <style>
    :root {
      --azul-principal: #05285C;
      --azul-circulo:  #3866A9;
      --verde-senz:    #0C326C;
      --cinza-bg:      #f8fafb;
      --branco:        #ffffff;
      --cinza-card:    #ffffff;
      --cinza-borda:   #e3e7ed;
      --cinza-txt:     #6c7b8a;
      --azul-btn:      #79c3e6;
      --azul-btn-hover:#5faed7;
      --sombra:        rgba(48,54,80,0.08);
    }
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: Arial, sans-serif; }
    html, body {
      width: 100%;
      height: 100%;
      background: var(--cinza-bg);
    }
    body {
      min-height: 100vh;
      min-width: 100vw;
      width: 100vw;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      position: relative;
    }
    .main-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
      max-width: 430px;
      margin: 50px auto 60px auto;
      gap: 0;
    }
    .header {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 12px;
    }
    .logo-text {
      color: var(--azul-principal);
      font-size: 2.1rem;
      font-weight: bold;
      letter-spacing: 2px;
      text-align: left;
    }
    .user-circle {
      width: 36px;
      height: 36px;
      border: 1.5px solid var(--cinza-borda);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--cinza-txt);
      font-weight: bold;
      font-size: 1.2rem;
      background: #f7fafd;
    }
    .greeting {
      font-size: 1.18rem;
      color: #222;
      font-weight: bold;
      width: 100%;
      text-align: left;
      margin-bottom: 10px;
      margin-top: 0;
    }
    .suggestion-card {
      background: #e8f1fa;
      border-radius: 14px;
      box-shadow: 0 2px 8px var(--sombra);
      padding: 18px 16px 20px 16px;
      margin: 0 0 18px 0;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
      gap: 8px;
    }
    .suggestion-card p {
      color: var(--azul-principal);
      font-size: 1.03rem;
      font-weight: 500;
      margin-bottom: 7px;
    }
    .suggestion-card button {
      background: var(--azul-btn);
      color: var(--branco);
      border: none;
      border-radius: 8px;
      padding: 10px 30px;
      font-size: 1.08rem;
      font-weight: bold;
      cursor: pointer;
      margin-top: 2px;
      box-shadow: 0 2px 10px var(--sombra);
      transition: background .18s;
    }
    .suggestion-card button:hover {
      background: var(--azul-btn-hover);
    }
    .metrics {
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 18px;
      margin-top: 0;
    }
    .metric-card {
      background: var(--cinza-card);
      border: 1.3px solid var(--cinza-borda);
      border-radius: 18px;
      box-shadow: 0 1.5px 7px var(--sombra);
      display: flex;
      align-items: center;
      padding: 17px 14px 15px 16px;
      gap: 14px;
      width: 100%;
    }
    .metric-card img {
      width: 52px;
      height: 52px;
      object-fit: contain;
      margin-right: 2px;
      background: #f4faff;
      border-radius: 50%;
      box-shadow: 0 1.5px 6px var(--sombra);
      padding: 4px;
    }
    .metric-info {
      display: flex;
      flex-direction: column;
      gap: 2px;
      flex: 1;
    }
    .metric-info .title {
      font-size: 1.15rem;
      font-weight: bold;
      color: var(--azul-principal);
      margin-bottom: 2px;
    }
    .metric-info .value {
      font-size: 1.19rem;
      color: #e94d60;
      font-weight: 600;
      margin-bottom: 2px;
      letter-spacing: 0.5px;
    }
    .metric-info .status {
      font-size: 0.99rem;
      color: var(--cinza-txt);
      font-weight: 500;
    }
    /* Chatbot */
    .chatbot-wrapper {
      position: fixed;
      right: 40px;
      bottom: 130px;
      display: flex;
      align-items: flex-end;
      z-index: 900;
      flex-direction: row-reverse;
    }
    .robot-icon {
      width: 58px;
      height: 58px;
      border-radius: 50%;
      background: #fff;
      box-shadow: 0 2px 10px var(--sombra);
      object-fit: contain;
      padding: 3px;
      margin-left: 10px;
      position: relative;
      top: 56px;
    }
    .bubble {
      background: var(--azul-principal);
      color: #fff;
      padding: 7px 36px;
      border-radius: 15px;
      box-shadow: 0 3px 14px var(--sombra);
      max-width: 225px;
      font-size: 1.04rem;
      font-weight: 500;
      line-height: 1.35;
      position: relative;
      margin-bottom: 0;
      text-align: left;
      margin-right: 25px;
      display: flex;
      align-items: center;
      min-height: 50px;
      bottom: 30px;
    }
    .bubble::after {
      content: "";
      position: absolute;
      right: -20px;
      top: 68%;
      transform: translateY(-30%) rotate(28deg);
      border-width: 15px 0 15px 20px;
      border-style: solid;
      border-color: transparent transparent transparent var(--azul-principal);
    }
    .dock {
      position: fixed;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 22px;
      background: #f7f7f7;
      padding: 12px 20px;
      border-radius: 20px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.1);
      z-index: 999;
    }
    .dock-icon {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background: #e3e3e3;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: transform 0.2s, background 0.3s;
      cursor: pointer;
    }
    .dock-icon:hover { transform: scale(1.15);}
    .dock-icon.active { background: #3866A9;}
    .dock-icon svg {
      width: 24px;
      height: 24px;
      fill: #05285c;
    }
    .dock-icon.active svg { fill: #fff; }
    @media (max-width: 800px) {
      .main-content { max-width: 98vw; }
      .metrics, .suggestion-card, .header, .greeting { max-width: 98vw;}
      .chatbot-wrapper { right: 6vw;}
      .bubble { font-size: 0.98rem; max-width: 56vw;}
    }
    @media (max-width: 500px) {
      .main-content { max-width: 100vw; margin-top: 14px; margin-bottom: 80px;}
      .header, .greeting, .suggestion-card, .metrics { max-width: 98vw;}
      .chatbot-wrapper { right: 5vw; bottom: 98px;}
      .bubble { max-width: 70vw; font-size: 0.95rem;}
      .robot-icon { width: 42px; height: 42px; }
      .dock { gap: 13px; padding: 8px 8px;}
      .dock-icon { width: 36px; height: 36px;}
      .dock-icon svg { width: 20px; height: 20px;}
      .suggestion-card { padding: 11px 6px 14px 6px; }
      .metric-card { padding: 10px 5px 8px 5px; gap: 9px;}
      .metric-card img { width: 38px; height: 38px; padding: 2px;}
      .metric-info .title { font-size: 1.03rem;}
      .metric-info .value { font-size: 1.07rem;}
      .metric-info .status { font-size: 0.93rem;}
    }
  </style>
</head>
<body>
  <div class="main-content">
    <div class="header">
      <div class="logo-text">SENZ</div>
      <div class="user-circle"></div>
    </div>
    <div class="greeting">Bem vindo(a) de volta, <br></div>
    <div class="suggestion-card">
      <p>Parece que sua resposta galvânica aumentou.<br>Que tal uma respiração guiada agora?</p>
      <button>Iniciar Exercício</button>
    </div>
    <div class="metrics">
      <div class="metric-card">
        <img src="./assets/img/cardio.png" alt="Frequência Cardíaca">
        <div class="metric-info">
          <div class="title">Nivel de Ansiedade</div>
          <div class="value">-- bpm</div>
          <div class="status">Estável</div>
        </div>
      </div>
      <div class="metric-card">
        <img src="./assets/img/galvanica.png" alt="Atividade Galvânica">
        <div class="metric-info">
          <div class="title">Atividade Galvânica (EDA)</div>
          <div class="value">0.4 μS</div>
          <div class="status">Elevado</div>
        </div>
      </div>
      <div class="metric-card">
        <img src="./assets/img/atividadesmentais.png" alt="Exercícios Mentais">
        <div class="metric-info">
          <div class="title">Exercícios Mentais</div>
          <div class="status">Último exercício: Respiração Box (Quadrada)</div>
        </div>
      </div>
    </div>
  </div>
  <div class="chatbot-wrapper">
    <img class="robot-icon" src="./assets/img/jarves.png" alt="Chatbot">
    <div class="bubble">
      <p>Olá, pronto para ajudar com foco e bem-estar.<br>Precisa de algo?</p>
    </div>
  </div>
  <div class="dock">
    <div class="dock-icon active" onclick="setActive(this)">
      <svg viewBox="0 0 24 24"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/></svg>
    </div>
    <div class="dock-icon" onclick="setActive(this)">
      <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 
        2 5.42 4.42 3 7.5 3c1.74 0 3.41 .81 4.5 2.09
        C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5
        c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
    </div>
    <div class="dock-icon" onclick="setActive(this)">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm1-13h-2v6h6v-2h-4z"/>
      </svg>
    </div>
    <div class="dock-icon" onclick="setActive(this)">
      <svg viewBox="0 0 24 24">
        <path d="M3 17h2v-7H3v7zm4 0h2V7H7v10zm4 0h2v-4h-2v4zm4 0h2V4h-2v13zm4 0h2v-9h-2v9z"/>
      </svg>
    </div>
    <div class="dock-icon" onclick="setActive(this)">
      <svg viewBox="0 0 24 24">
        <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4
          -4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8
          1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
      </svg>
    </div>
  </div>
  <script src="https://www.gstatic.com/firebasejs/9.22.2/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.22.2/firebase-auth-compat.js"></script>

<script>
  // Sua configuração Firebase (substitua pelos seus dados)
  const firebaseConfig = {
            apiKey: "AIzaSyBOqDbKnGTkP-35Hjsga8hnPQFqQRp7AME",
            authDomain: "senz-bae74.firebaseapp.com",
            projectId: "senz-bae74",
            storageBucket: "senz-bae74.appspot.com",
            messagingSenderId: "604865943153",
            appId: "1:604865943153:web:b17d947e686a5becc4add0",
            measurementId: "G-3GNNQWFVGH"
        };
  firebase.initializeApp(firebaseConfig);

  function setActive(el) {
    document.querySelectorAll('.dock-icon').forEach(icon => {
      icon.classList.remove('active');
    });
    el.classList.add('active');
  }

  // Atualiza o nome e foto do usuário no header
  firebase.auth().onAuthStateChanged(user => {
    if (user) {
      const nome = user.displayName || {nome};
      const foto = user.photoURL;

      // Atualiza saudação
      const greeting = document.querySelector('.greeting');
      if (greeting) {
        greeting.innerHTML = `Bem vindo(a) de volta,<br>${nome}`;
      }

      // Atualiza círculo do usuário
      const userCircle = document.querySelector('.user-circle');
      if (userCircle) {
        if (foto) {
          userCircle.innerHTML = `<img src="${foto}" alt="Foto de ${nome}" style="width:100%; height:100%; border-radius:50%;">`;
          userCircle.style.border = "none";
          userCircle.style.background = "transparent";
          userCircle.style.color = "transparent";
        } else {
          userCircle.textContent = nome.charAt(0).toUpperCase();
          userCircle.style.border = "1.5px solid var(--cinza-borda)";
          userCircle.style.background = "#f7fafd";
          userCircle.style.color = "var(--cinza-txt)";
        }
      }
    } else {
      // Se não estiver logado, redireciona para login
      window.location.href = "index.html";
    }
  });

  // --- Atualiza BPM do Firebase ---
  const firebaseSecret = "eGvu0jhnP7YMRVnuj4jGKxXmpq4fVW6x087THsVq";
  const userId = localStorage.getItem('firebaseUid');

  function getBpmValueElement() {
    const metricCards = document.querySelectorAll('.metric-card');
    for (let card of metricCards) {
      if (card.textContent.includes('Nivel de Ansiedade')) {
        return card.querySelector('.value');
      }
    }
    return null;
  }

  async function buscarUltimoBPM() {
    const bpmValueEl = getBpmValueElement();
    if (!bpmValueEl) {
      console.warn("Elemento de BPM não encontrado.");
      return;
    }
    if (!userId) {
      bpmValueEl.textContent = "-- bpm";
      alert("Usuário não identificado. Faça login novamente.");
      window.location.href = "index.html";
      return;
    }
    const firebaseUrl = `https://senz-bae74-default-rtdb.firebaseio.com/users/${userId}/batimentos.json?auth=${firebaseSecret}`;
    try {
      const res = await fetch(firebaseUrl);
      const data = await res.json();

      if (!data || Object.keys(data).length === 0) {
        bpmValueEl.textContent = "-- bpm";
        return;
      }
      let ultimoBpm = null;
      let ultimoTimestamp = -1;
      Object.values(data).forEach(item => {
        if (item && item.timestamp > ultimoTimestamp) {
          ultimoTimestamp = item.timestamp;
          ultimoBpm = item.bpm;
        }
      });
      if (ultimoBpm !== null) {
        bpmValueEl.textContent = ultimoBpm + " bpm";
      } else {
        bpmValueEl.textContent = "-- bpm";
      }
    } catch (e) {
      bpmValueEl.textContent = "-- bpm";
      console.error("Erro ao buscar BPM:", e);
    }
  }

  buscarUltimoBPM();
  setInterval(buscarUltimoBPM, 10000);
</script>
</body>
</html> 
